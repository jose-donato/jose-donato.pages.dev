---
import Icon from "./Icon.astro";
import { SOCIALS } from "../utils/constants";
import Floater from "./Floater.astro";
import { Image } from "astro:assets";
---

<div class="flex flex-col md:flex-row items-center gap-12 mb-8">
  <div class="flex flex-col flex-1">
    <h1 class="text-2xl md:text-3xl tracking-tight font-light mb-2">
      Hello! My name is
      {" "}
      <span class="font-bold text-bullish-500">José Donato</span>{" "}<span
        class="wave">👋</span
      >
    </h1>
    <div
      class="flex flex-col gap-4 text-base text-text-secondary leading-relaxed mt-6"
    >
      <p>
        I'm a <strong class="text-text-primary">Fullstack Engineer</strong> from
        Portugal, specializing in building tools for quants and traders. Currently
        leading web development at <strong class="text-bullish-500"
          >OpenBB</strong
        >, I've architected and built OpenBB Workspace from the ground up over
        the past 4 years. My passion lies at the intersection of <span
          class="underline-animation text-text-primary font-medium"
          >trading</span
        > and <span class="underline-animation text-text-primary font-medium"
          >web technology</span
        >.
      </p>
      <p class="text-text-muted">
        Want to have a quick chat? You can DM me on Twitter or LinkedIn.
      </p>
    </div>
    <div class="flex gap-4 mt-8 items-center">
      {
        SOCIALS.map((social) => (
          <a
            aria-label={social.name}
            class="icon-btn hover:scale-110 hover:bg-bullish-600/10"
            href={social.url}
            target="_blank"
            rel="noopener noreferrer"
          >
            <Icon id={social.icon} />
          </a>
        ))
      }
    </div>
  </div>
  <div class="flex md:min-w-[240px]">
    <Image
      src="/avatar.webp"
      class="h-48 w-48 object-cover shadow-2xl ring-2 ring-border-primary/20"
      alt="José Donato"
      width={192}
      height={192}
    />
  </div>
</div>
<div class="divider my-8"></div>
<Floater />

<style>
  .underline-animation {
    position: relative;
    padding-bottom: 3px;
  }

  .underline-animation::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--bullish-500);
    transform: scaleX(1);
    transform-origin: left;
    transition: all 0.3s ease;
  }

  .underline-animation:hover::after {
    background: var(--bullish-400);
    height: 3px;
  }
</style>

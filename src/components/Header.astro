---
import Icon from "./Icon.astro";
import { SOCIALS } from "../utils/constants";
import Floater from "./Floater.astro";
import { Image } from "astro:assets";
---

<div class="flex flex-col md:flex-row items-center gap-10">
  <div class="flex flex-col">
    <h1 class="text-xl tracking-wide">
      Hello! My name is
      {" "}
      <span class="font-bold">José Donato</span>{" "}<span class="wave">👋</span
      >
    </h1>
    <div class="flex flex-col gap-1 text-sm text-text-muted mt-4">
      <p>
        I'm a Fullstack Engineer from Portugal, specializing in building tools
        for quants and traders. Currently leading web development at OpenBB,
        I've architected and built OpenBB Workspace from the ground up over the
        past 4 years. My passion lies at the intersection of <span
          class="underline-animation">trading</span
        > and <span class="underline-animation">web technology</span>.
      </p>
      <p>Want to have a quick chat? You can DM me on Twitter or LinkedIn.</p>
    </div>
    <div class="flex gap-3 mt-4 items-end">
      {
        SOCIALS.map((social) => (
          <a
            aria-label={social.name}
            class="icon-btn"
            href={social.url}
            target="_blank"
            rel="noopener noreferrer"
          >
            <Icon id={social.icon} />
          </a>
        ))
      }
    </div>
  </div>
  <div class="flex md:min-w-[20%]">
    <Image
      src="/avatar.webp"
      class="rounded-md h-40 object-cover shadow-lg"
      alt="me"
      width={160}
      height={160}
    />
  </div>
</div>
<div class="divider my-4"></div>
<Floater />

<style>
  .underline-animation {
    position: relative;
    padding-bottom: 2px;
  }

  .underline-animation::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background-image: linear-gradient(transparent, transparent),
      linear-gradient(var(--bullish-500), var(--bullish-500));
    background-size: 100% 2px;
    background-position: 0 100%;
    background-repeat: no-repeat;
    rotate: -1deg;
    transition: all 0.3s ease;
  }

  .underline-animation:hover::after {
    background-image: linear-gradient(transparent, transparent),
      linear-gradient(var(--bullish-400), var(--bullish-400));
  }
</style>
